# 交接：showcase 插图占位（方案B）— 20260916

> 执行设备：另一台设备（管理机）。本文件与修复脚本 `scripts/localize-spark-images.mjs` 同提交，`git pull` 即得。

## 一、背景（30 秒版）

- `c5a2404` 已把 11 个秒搭建作品网页的 React/Babel SDK 死链本地化，**页面文字框架已全部正常渲染**。
- 残留最后一层：**7 个页面共 49 处插图引用**（去重 38 张图）指向秒搭平台存储接口 `/spark/app/<app_id>/runtime/...`，平台接口对未登录请求 302 到 `service-not-activated`，**无法脚本抓取**；本机与 git 历史均无源图。
- 已确认采用**方案B**：按各页设计主题生成风格化 SVG 占位图，本地化后页面完全离线可用。日后拿到平台真图，直接同名替换即可（见第五节）。

## 二、执行（在仓库根目录，一条命令）

```bash
git pull --rebase          # 拿到本脚本与本文件（不低于含 scripts/localize-spark-images.mjs 的提交）
node pg-keeper.mjs ensure  # 可选：确保服务在跑（纯静态改动其实无需重启）
node scripts/localize-spark-images.mjs
```

**预期输出**：7 个页面各打印一行（amber 8 / cirque 7 / dunhuang 6 / hanabi 1 / monsoon 4 / oilpaper 6 / paper-kite 6），末行
`✅ 完成：7 个页面，38 张 SVG 占位图，49 处引用改写，0 残留`。
脚本自带残留断言，改写不干净会以非零码退出。重复执行是 no-op（幂等）。

## 三、验证（三步）

1. `grep -rl "/spark/" public/showcase/*/index.html || echo 无残留` → 应输出「无残留」。
2. `curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8787/showcase/paper-kite-museum/img/illu-01.svg` → 应为 200。
3. 浏览器（建议 Ctrl+F5 强刷）抽查三张最重的页面：`/showcase/paper-kite-museum/`、`/showcase/amber-insect-archive/`、`/showcase/oilpaper-umbrella/` —— 插图位置应显示与页面配色一致的抽象 SVG 占位（渐变+圆弧+页名），无破图。已在主设备实测：占位图与设计融合自然。

## 四、提交与推送

```bash
git add public/showcase/
git commit -m "feat(showcase): 方案B——7页49处平台图片引用本地化为风格化SVG占位图"
git push
```

**回滚**（如需）：`git checkout -- public/showcase && git clean -fd public/showcase/*/img`——脚本只动 `public/showcase/` 下的 7 个 index.html 和新增的 `img/` 目录，不碰任何其他文件。

## 五、日后换真图（重要性质）

占位图文件名固定为 `public/showcase/<slug>/img/illu-NN.svg`，HTML 引用不会再改。
从秒搭平台导出真图后，**按原顺序同名覆盖**（svg 换成同名 .svg，或改扩展名后同步改 HTML）即可，无需重跑脚本。
平台导图对应关系见主设备 `build/spark-urls.txt`（38 个文件 id 清单，未入库；需要时可重新枚举）。

## 六、挂起事项（均等你发话，不阻塞本次执行）

1. 6 个资源 `user_id=null`（谁的「我的资源」都看不到）——待定归属或改公共可见。
2. 死代码 `/api/works/gallery` + 旧注释清理。
3. 6 件图片类作品（id 144–149）无原网页链接，属设计如此；若要改按钮文案另说。
4. 防火墙：主设备已放行 8787（规则 `livzon-ai-8787`）；本机若只本机自用无需处理。
