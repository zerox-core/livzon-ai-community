# qa-bot 知识库自检

问答机器人（`server/qa-bot`）的知识库检索自测脚本。改动 `server/qa-bot/kb.js` 或 `server/qa-bot/kb/*.md` 后跑一遍，确认用户问题都能命中正确的知识库文档。

## 用法

```bash
node tests/qa-bot/kb-test.cjs
```

跑完在本目录生成 `kb-test.out`，逐条列出每个测试问题命中的文档、得分、所属区（active=现役 / archive=档案）与身份牌（year/status）。

## 覆盖的测试问题

- 现在有什么活动 → 应命中「活动总览」
- 最近有什么比赛 → 应命中「活动总览」或「比赛评审」
- 比赛有奖励吗 → 应命中「比赛评审」
- 我提交的作品什么时候评审 → 应命中「比赛评审」
- 我能不能私信举办方交作品，我不会用平台 → 应命中「作品提交」
- 网页设计马拉松怎么报名 → 应命中「网页设计马拉松 9月赛季」
- 美术赛什么时候截稿 → 应命中「美术资源设计赛 秋季场」
- 怎么下载作品 → 应命中「作品巨幕」
- 我是新手，平台怎么用 → 应命中「平台新手指南」
- 投票一天能投几票 → 应命中「作品巨幕」
- 去年这个比赛是什么规则 → 时间感检索：有档案馆时应切 archive 区只搜往届资料；暂无档案时回退现役区

## 最近一次验证（2026-09-15，13 篇知识库文档）

8 个测试问题全部命中预期文档：

```
Q: 我提交的作品什么时候评审     -> 比赛评审    score=18.5
Q: 我能不能私信举办方交作品...   -> 作品提交    score=20.5
Q: 网页设计马拉松怎么报名       -> 网页设计马拉松 9月赛季  score=29
Q: 美术赛什么时候截稿          -> 美术资源设计赛 秋季场   score=14.5
Q: 怎么下载作品               -> 作品巨幕    score=10
Q: 我是新手，平台怎么用        -> 平台新手指南  score=19
Q: 投票一天能投几票           -> 作品巨幕    score=5.5
Q: 去年这个比赛是什么规则       -> 回退现役区（档案馆暂为空）
```

知识库写入规范见 `server/qa-bot/kb/` 各文档的身份牌（year/status）与归档目录 `kb/archive/<年份>/`。

## LLM 网关连通性自测

```bash
node tests/qa-bot/test-llm.cjs
```

直接调 `server/qa-bot/llm.js` 的 chat 走一遍真实网关（读取 `server/.env` 的 LLM 配置），输出 `LLM_OK`（含模型名和回复摘要）或 `LLM_FAIL`（含错误原因），不打印任何密钥。改完 `.env` 里的 `QA_BOT_LLM_*` 配置后先跑这个再重启机器人。
